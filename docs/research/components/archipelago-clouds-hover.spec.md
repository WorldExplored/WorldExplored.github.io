# Clouds and stable destination ownership
Targets clouds.ts, Landmark.tsx, new meaningful cloud/hover tests. AmbientSystem integration changes must be supplied as a patch to root, not edited concurrently. Parent owns CSS change to labels pointer-events none.
Clouds: >=5 visibly different silhouettes using different puff counts and graphs: broad layered, tall cauliflower, cotton, long bank, thin atmospheric. Add optional family. Distinguish depth, altitude, density, azimuth, scale, speed, shape. Prominent first 10 clusters must include every archetype and be differently posed. World extends x +/-100 z -120...40; foreground/mid clouds around y16...27, city clouds farther. No fixed count six assumption. Export instance range/count helper for renderer integration. Preserve exact rendered ellipsoid hit union incl resting deformed volume, nearest first ownership, smooth bounded damping and shared response. No sudden growth/jumps. Practical clouds interactive.
Landmark: fixed complete invisible hit proxy per destination, no changing geometry or scale hit boundary. Model child changes cannot clear label. Exit grace ~120ms, reentry cancels, unmount cleanup, no touch sticky hover. Remove building pulse scale so geometry is stable. Keyboard label remains parent-owned independent. Click supports delta threshold and no accidental navigation after dragging. Parent positions are configured separately. Radii Work5.5 Research3.8 Purdue1.8 About3.5 Contact3.2 Building2.4; top <=6.5,4.8,2.8,4.2,4.2,7.5 respectively, local floor .8.
Regression: hold pointer for >=3 actual seconds on each proxy and verify one hover appearance/no toggles; include crossing child meshes, grace reentry, leave and touch. Add tests to existing scripts or tell root needed package script change. Existing DOM .landmark CSS uses 160ms transitions, opacity0/1. Parent will change pointer-events to none always. No content changes. Screenshot private work/archipelago/before/desktop.jpg. Run strict TS and relevant tests.
