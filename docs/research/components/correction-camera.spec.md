# Single free camera correction
Own CameraDirector.tsx, optional cameraControls.ts helpers, camera-specific tests in new scripts/camera.test.tsx. Do not touch world.ts, renderer, UI, audio. Existing runtime fields may be reused; root can add fields on request.
Delete scroll route. Use one bounded orbit/zoom/pan system with subtle pointer parallax. Wheel over canvas zooms toward cursor terrain ray; touch pinch zooms, empty-world drag orbits (right drag pans optionally). Avoid drag conflicts with ReflectiveObject using runtime.dragging and target hit filtering: architecture may click, empty ground can orbit. Pointer tracking canvas entire area incl transparent identity; UI surfaces/dock intercept. Wheel over reading surface native internal scroll. Dock chooses destination with 800ms smooth cancelable focus, then free controls remain available. On new destination focus once, never reset user pose per frame or panel state. For desktop selected object compose on left with content right; mobile selected object above bottom sheet. Maintain initial framing per aspect ratio, responsive update only if untouched.
Use installed three-stdlib OrbitControls if available through drei to get proven wheel/pinch, or explicit bounded controls. Clamp distance e.g8–85, polarangle .28–1.32, camera y>=3.5, prevent entering all structures using conservative sphere/cylinder footprints: work[-8,0,0]radius4.4 height6, research[4,0,-7]radius3.2 height4, Purdue[13,0,-12]radius1.8 height3, About[-10,0,11]radius3.2height4,Contact[12,0,8]radius2.8height4,lamp[1,0,-65]radius2.2height6. Do not enter land/water. Keep target region bounded. Reduced motion removes inertia/flights/parallax but deliberate zoom/orbit/navigation still functional. Always invalidate demand when controlschange. Never manualrender or rootconfigure. Use default Fiber render.
Tests camera bounds, zoomcursor, constrainedorbit/pinch mapping, focus cancellation/arrival and independent view after focus, reduced behavior. Add via root package script later. Must compile; do not commit.
