# Landmark architecture specification

Target: `src/components/world/LandmarkModels.tsx` only.
Export `LandmarkModel({ id, active, runtime, paused, quality })` with id LandmarkId, active boolean, runtime MutableRefObject<SceneRuntime>, paused boolean, quality QualityTier, all types from src/content/world.ts except React ref.
The caller places each model at world.landmarks.position. Local water level y=0. Terrain under work/purdue rises to y=.9. Model bases at y=1. No DOM, labels, navigation, scene lights or environment here. Parent owns landmark event handlers.
Original Aero composition studied in the baseline (docs/design-references existing imagery); use original procedural geometry, no third-party images/models, no fetched assets or new dependencies.
Palette from world.colors: porcelain #f5fff4, glass #7ee6e7, cyan #56e4ee, lime #a7ed61, restrained gold #ddb858. Glossy clearcoat/metal surfaces, rounded dense smooth geometry, no flat low-poly appearance. Main camera [15,15,32], target [0,3,-3], perspective fov43; likely visible upper/right sides. All models must also look good from front close-up and mobile far camera.

Work: recognizable glass-domed GPU observatory, total width7, height5.7. Circular stepped white plinth radius3.2 at y1, aqua underlit skirt. Glass hemisphere radius2.8, 4 polished structural ribs; use clearcoat transparent material, avoid expensive transmission passes. Inside: central lime GPU chip with metallic pin contacts and three slim glass server/data columns. Polished torus orbital data rings and a few animated bright pulses around them. Ribs visible over glass, distinct opaque silhouette beneath. Door/entrance facing camera and short curved or radial steps.
Research: sculpted open book of glass/porcelain, width4.5 on a stepped circular lagoon platform radius2.7 at y.4. Book spine y1.1, curled open pages rising to y2.7; custom smooth BufferGeometry or extruded curved shape, 4 thin layered pages visible on each side, aqua tinted cover. Fine parallel lines suggest typeset pages without embedding text. Two slim orbital arcs around the pedestal with a moving cyan bead; no random particles here.
Purdue: optimistic rounded pavilion width5.8, total height5.5; stepped circular porch radius3, six white columns with simple capitals, a restrained gold shallow dome on white rim, open center. Polished inlaid gold band and small finial. Abstract architecture, no official logo or invented affiliation label. Slightly rotating gold orbit above finial optional; don't over-animate entire architecture.
Building: restrained distant signal tower height4.8, width1.2, circular base radius1.5. White tapering shaft, cyan glass globe/lens and rings near top; one subtle light sweep every ~8seconds via fading rotating cone/arc, small moving light only. No text/details about project.

Hover active brightens emissive trim/material, scale at most1.02, transition via frame delta damping ~180ms. Never move model bases. Ambient signals move using runtime.current.elapsed; useFrame returns immediately when paused. Reduced-motion renders fallback so these are unmounted, but paused also must freeze values without resetting.
Reuse repeated geometries/materials (one shared mesh material per color). Aim total architecture <140 draw calls before shadow pass and <180k triangles high tier. Segments use world.quality; cylinders/torus smooth enough even low. Consider instancing pins/columns/repeated book lines. Do not use many point lights. Cast shadows on principal opaque structures; transparent glass should not cast solid black shadows. Dispose owned memoized resources on unmount; no per-frame allocations or React state updates. Use strict TS/named exports/2spaces.
No product copy in component. Shared world config controls colors and transforms. Any added architectural numeric parameters can be local constants named for meaning.
Verify `npx tsc --noEmit` and report resources/draw call estimate and files. Commit only this file in assigned worktree when verified. Do not push. Parent integrates and visually checks.
